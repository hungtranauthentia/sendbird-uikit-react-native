import type { ReactNode } from 'react';
import type { FastImageProps } from 'react-native-fast-image';
declare let FastImageInternal: (props: FastImageProps) => ReactNode | null;
export default FastImageInternal;
