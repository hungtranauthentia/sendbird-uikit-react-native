import React from 'react';
import { StyleProp, ViewStyle } from 'react-native';
type Props = {
    style?: StyleProp<ViewStyle>;
    space?: number;
};
declare const Divider: ({ style, space }: Props) => React.JSX.Element;
export default Divider;
